using CommunityToolkit.Mvvm.ComponentModel;

namespace NvimManager.App.ViewModels;

public abstract class ViewModelBase : ObservableObject
{
    // Deliberately thin: hooks for logging / diagnostics can live here later.
}